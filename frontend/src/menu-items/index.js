import dashboard from './dashboard';
import pages from './pages';
import categories from './categories';
import other from './other';

// ==============================|| MENU ITEMS ||============================== //

// const menuItems = {
//   items: [dashboard, pages, categories, other]
// };

const menuItems = {
    items: [categories]
  };

export default menuItems;
